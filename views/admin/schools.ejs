<div class="panel panel-default scrollable">
<table class="table table-hover table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>SAS Name</th>
        </tr>
    </thead>
    <tbody>
        <% for(var c = 0; c < schools.length; c++) { %>
        <tr>
            <td><%= schools[c].name %></td>
            <td><%= schools[c].sas_name %></td>
        </tr>
        </a>
        <% } %>
    </tbody>
</table>
</div>

<button style="float: right" type="button" class="btn btn-default btn-sm" data-href="/user/schools/edit">
<span class="glyphicon glyphicon-pencil"></span> Edit
</button>
