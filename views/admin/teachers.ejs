<div class="panel panel-default scrollable">
    <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>SAS Room Num</th>
                <th>School</th>
            </tr>
        </thead>
        <tbody>
            <% for(var t = 0; t < teachers.length; t++) { %>
            <tr>
                <td><%= teachers[t].name %></td>
                <td><%= teachers[t].email %></td>
                <td><%= teachers[t].room_num %></td>
                <td><%= teachers[t].school_name %></td>
            </tr>
            </a>
            <% } %>
        </tbody>
    </table>
</div>

<button style="float: right" type="button" class="btn btn-default btn-sm" data-href="/user/teachers/edit">
    <span class="glyphicon glyphicon-pencil"></span> Edit
</button>
