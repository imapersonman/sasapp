<div class="panel panel-default scrollable">
    <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Email</th>
            </tr>
        </thead>
        <tbody>
            <% for(var s = 0; s < r_class.students.length; s++) { %>
            <tr data-href=<%= "/user" %>>
                <td><%= r_class.students[s].name %></td>
                <td><%= r_class.students[s].email %></td>
            </tr>
            </a>
            <% } %>
        </tbody>
    </table>
</div>
